name: vesc_telemetry
description: Typed telemetry value store with per-key streams for VESC data.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^4.0.0
