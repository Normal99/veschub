name: templates
description: Starter dashboard templates for Template (Basic) mode.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  dashboard_model:
    path: ../dashboard_model

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
