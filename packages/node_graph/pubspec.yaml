name: node_graph
description: Dataflow editor for Expert binding mode — wire telemetry through math/conditional/logic blocks.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  dashboard_model:
    path: ../dashboard_model

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^4.0.0
